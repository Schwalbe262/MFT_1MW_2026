# MFT 1 MW handoff — 2026-07-13

This is the committed handoff for the AEDT attach, 1K101 fill-factor, scheduler,
controller and data-pipeline work. The larger runtime copy is also available at
`Y:\git\MFT_1MW_2026\docs\HANDOFF_20260713_AEDT_1K101.md`.

## Ground rules

- The user's final instruction is Y-only work. Earlier RaiDrive read-only notices were
  explicitly overridden. Do not move source development to C. C is used only by the
  already-installed Windows Python virtual environment.
- The main `Y:\git\MFT_1MW_2026` checkout is heavily dirty. Cluster jobs use detached,
  exact GitHub SHAs; uncommitted local changes are not deployed.
- The overall goal is not complete. At handoff, strict-full data is 1014 / 3000 and the
  old standalone controller is intentionally stopped.

## Live scheduler

- URL: `http://127.0.0.1:8000`
- exact live worktree: `Y:\git\slurm_scheduler_node_canary_260713`
- exact live code SHA: `bf273f4121356c0175de106375c14d1ee01619f6`
- branch: `experiment/aedt-node-canary-260713`
- scheduler handoff/docs tip: `b5f8e725ec6810dad19f80bdedfd6089348eece2`
- detailed scheduler handoff:
  `Y:\git\slurm_scheduler_node_canary_260713\docs\aedt_pool_handoff_20260713.md`
- launcher: `Y:\runtime\slurm_scheduler\start_web_y.cmd`
- runtime config/DB/logs: `Y:\runtime\slurm_scheduler`
- only auto launcher: Windows Scheduled Task `SlurmSchedulerWebNative`
- Windows Startup duplicate was removed because two infinite launchers raced and reset
  the in-memory license snapshot.
- runtime `accounts.yaml` private keys use absolute
  `Y:/git/slurm_scheduler/secrets/cluster/<account>.pem` paths. Do not revert to a clean
  worktree-relative `secrets/...` path.

Live scheduler feature chain:

- `9f7b005`: `aedt_backend=standalone|pooled`, independent of `fea_bursty`; generic N.
- `053cb4d`: generic-N node-local canary host.
- `7950150`: main dashboard `AEDT used/max` plus project usage.
- `d66cf54`: bounded node-local pooled-canary admission.
- `f22e3b1`: license refresh before fallible allocation stages.
- `bf273f4`: python3 bootstrap for raw host payloads.

At the final check the license snapshot was valid and EDT usage was about 330 / 550
(fluctuates). Central pool stays disabled/non-operational; UI canary limits are max AEDT 2,
target projects 4, projects/AEDT 2.

Health commands:

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/health
Invoke-RestMethod http://127.0.0.1:8000/api/licenses
Invoke-RestMethod http://127.0.0.1:8000/api/aedt-pool
Invoke-RestMethod http://127.0.0.1:8000/api/projects/MFT_1MW_2026v1
```

## AEDT attach status

Completed evidence:

- `30089`: 1 host / 1 real MFT Matrix client passed, close/license cleanup passed.
- `30445`: 1 host / 2 real Matrix clients passed; pre-solve client abort did not break
  the sibling. Both valid results had `Llt=31.6777785986 µH`.
- `30474`: active-solve timeout isolation passed; sibling result valid, third client
  rejected, faulted Desktop recycled and license returned.
- MFT generic-N canary SHA: `78d9511`; standalone remains default.

Compute nodes cannot reach the Windows API. Probe `30475` timed out against
`100.112.168.31`, `192.168.0.3` and `192.168.56.1`, all on port 8000. Use the node-local
loopback control plane, not a central scheduler URL from compute nodes.

Latest host attempt:

- `30484` opened AEDT 2025.2 on n116 in 16.2 seconds.
- Desktop PID 1052278, host PID 1051935, persistence PID 1052342, gRPC port 45193,
  loopback control plane `127.0.0.1:36359`, N=2.
- It then failed because ordinary simulation anchor `30371` completed; the host had been
  submitted with `same_node_as_task_id=30371`.
- Clients `30485` and `30486` therefore failed before launch, with no simulation/license
  side effects.
- Cleanup was completed on Slurm job 732393. The three canary PIDs, port 45193, task
  marker and `/tmp` bundle files were verified absent; license snapshot remained valid.

Next canary must not anchor the host to a short simulation. Submit the high-priority host
with `same_node_as=0` on a normal warm allocation, then submit clients with
`same_node_as=<host task>`. Clients must use `fea_bursty`, `aedt_backend=pooled`, exact
MFT SHA `78d9511`, explicit conda env setup and matching generic-N bundle identity. Require
two terminal result rows, close ACKs and before/during/after license evidence.

## Attach-aware refill controller

- Branch: `codex/pooled-refill-controller-260713`
- final pushed SHA: `3106b0d16867394d9e193d38afd9653806dd8835`
- worktree: `Y:\git\MFT_pooled_refill_controller_260713`
- 21 new tests passed.

It supports logical project target 300, generic N and validated-N ceiling, AEDT session
ceiling, standalone/pooled `fea_bursty`, bundle `expected_rows=N`, missing-row-only
standalone retry using a new identity, no mass-cancel authority and revision-scoped
provenance/dedupe. It is prepared only; do not start it until the final combined attach
plus 1K101 solver revision is selected.

## 1K101 / fill-factor status

- Branch: `experiment/1k101-native-lamination-ab-20260713`
- last known pushed SHA: `336ab7df64a25664cacab260f2ebcbe72fe2d942`
- native Lamination setup and Matrix solves reached solved state for kf 1.00/0.85/0.70.
- last Matrix times were about 9m30 / 5m21 / 2m35.
- `30473` kf 0.70 failed only in the Bpow postprocessor after Matrix.
- `30471`/`30472` were cancelled after preserving solved output; `30478` was cancelled
  because a direct diagnostic was used.
- direct evidence (verify by SSH):
  `/home1/jji0930/slurm_scheduler/runs/mft_bpow_direct_s336ab7d_r3.log`
- `analyze_dispatched=false`; `EnterQty(B)` passed in ~0.0005 s, then
  `CalcOp(ComplxPeak)` immediately terminated the AEDT macro/gRPC operation. Division,
  fractional power and integration were never reached.
- saved kf 0.85 solution (verify by SSH):
  `/home1/jji0930/slurm_scheduler/runs/mft_postprocess_snapshot_30472_s85e0963`
- direct diagnostic was closed and its license returned.

Do not use AEDT Calculator as the equation truth source. Implement a pure Python reference:

```text
B_pack = Faraday(Vrms, f, N, gross area)
B_material = B_pack / kf
specific loss = 6.5 * f_kHz^1.51 * B_material^1.74
raw loss = specific loss * effective alloy mass
margin loss = raw loss * configured margin
```

Compare it with Maxwell native Lamination B/CoreLoss. Use raw field export only as an
optional distribution check. Local uncommitted attempts in `module/core_material_contract.py`
and `run_simulation_260706.py` were incomplete/untested and are not on GitHub; inspect or
discard them. Reuse saved kf 0.85 and, if present, task-30473 kf 0.70 solutions before any
new Matrix run.

If kf=0.70 passes, create a new revision such as
`mft1mw-1k101-native-lamination-kf0p70-v3`; include kf explicitly in params/dedupe. New
B/core/total-loss and dependent thermal targets must not mix with legacy rows. Llt/winding
reuse requires target-aware cohort metadata.

## Data/monitoring/controller

- monitoring port: 8010
- monitoring live SHA/worktree: `12bcf82be2649488b96f85e5926778ec914c94f1` /
  `Y:\git\MFT_monitoring_throughput_12bc`
- dataset state recorded: raw 1543, strict EM 1087, strict full 1014, quarantined 529.
- activation target: strict full 3000.
- old strict identity: solver `b171c7c`, library `e6b9b9d`.
- MFT project cap: 300; logical active had decayed to about 134 because the old controller
  was intentionally stopped.
- cycle 467 was reconciled committed for accepted tasks 30469/30470; evidence SHA
  `f32bb28fbec01506663a1a2b0000c8c578806963d811c23b90bc152f2b6cc852`.
- Do not restart the old `run_controller_6a870.py`; it pins b171 and would refill old
  material data.

Immediate order:

1. Run one independent-lifetime N=2 pooled canary to terminal client rows and cleanup.
2. Finish pure-Python 1K101 reference and saved-solution comparison.
3. Make one combined attach plus approved-1K101 MFT revision and target-aware data contract.
4. Run controller `3106b0d` in read-only plan mode, review identities/capacity.
5. Start rolling refill toward 300 with no mass cancellation.
6. Confirm strict-full growth and continue to 3000.
7. Consolidate Git branches only after stability; do not delete SHAs still referenced by
   tasks/evidence.

