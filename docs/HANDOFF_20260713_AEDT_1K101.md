# MFT 1 MW campaign handoff — AEDT attach / 1K101 / data pipeline

Updated: 2026-07-13 KST

## 0. Authoritative operating rule

- The user's final filesystem instruction is **work on Y only**. The earlier RaiDrive
  read-only notice was explicitly overridden by the user (`다시 Y: 건드려도 돼`,
  `다 Y에서 진행해 C에서하는건 없는걸로`).
- Source, Git worktrees, runtime DB/config/logs and evidence must stay on Y.
- C is currently used only as the already-installed Python virtual-environment
  interpreter for the Windows scheduler process. Do not move source development to C.
- Do not treat the dirty checkout `Y:\git\MFT_1MW_2026` as deployable. Cluster tasks
  fetch and detach exact GitHub SHAs. Uncommitted files in the dirty checkout are not
  present on the cluster.

## 1. Current high-level state

The overall goal is **not complete**.

| Item | Current evidence |
|---|---|
| Scheduler API | `http://127.0.0.1:8000`, healthy at handoff |
| Monitoring WEB | `http://127.0.0.1:8010`, alive |
| MFT project cap | 300 |
| MFT logical active at handoff | 134 running, 0 queued (controller intentionally stopped) |
| Dataset | `regression_260707/data/dataset/train.parquet` |
| Raw / strict EM / strict full | 1543 / 1087 / 1014 |
| Model activation target | strict full 3000 |
| Trained checkpoint | checkpoint 1000, parity/CV 18 of 18; not activated because 1014 < 3000 |
| AEDT attach production | implementation/canary infrastructure deployed, but no two real pooled MFT clients have completed yet |
| 1K101 | native lamination geometry/Matrix solves work; independent physics gate is unfinished |

The old standalone refill controller was stopped on purpose while the attach backend and
1K101 revision are being decided. Do not simply restart it: it pins solver `b171c7c` and
would refill with the old material revision.

## 2. Live Windows scheduler

### Exact live source

- Worktree: `Y:\git\slurm_scheduler_node_canary_260713`
- Branch: `experiment/aedt-node-canary-260713`
- Live/pushed SHA: `bf273f4121356c0175de106375c14d1ee01619f6`
- Launcher: `Y:\runtime\slurm_scheduler\start_web_y.cmd`
- Config: `Y:\runtime\slurm_scheduler\config\app.yaml`
- Accounts: `Y:\runtime\slurm_scheduler\config\accounts.yaml`
- Log: `Y:\runtime\slurm_scheduler\logs\web.log`
- Runtime DB/config/logs are on Y. The launcher imports the exact clean Y worktree.

Relevant commit chain:

- `9f7b005927bb4d039c743623e8f39e9a214feca0`: generic task/project
  `aedt_backend=standalone|pooled`, independent of `scheduling_profile=fea_bursty`.
- `053cb4d`: generic-N node-local canary host.
- `7950150`: AEDT usage on the main dashboard (`AEDT used/max`, project usage below it).
- `d66cf54`: bounded node-local pooled canary admission.
- `f22e3b1`: license refresh occurs before fallible allocation lifecycle stages;
  unavailable-account storage checks fail closed without aborting the tick.
- `bf273f4`: Python bootstrap for python3-only host nodes.

Tests recorded during development:

- selector full suite: 392 passed plus 27 subtests.
- node-local AEDT focused tests: 37 passed.
- payload bootstrap follow-up: scheduler `test_core` 347 passed.

### Single automatic launcher

There were two launchers (Windows Startup plus Scheduled Task), which raced and repeatedly
reset the in-memory license snapshot. The Startup file was removed. Only this must remain:

- Scheduled Task: `SlurmSchedulerWebNative`
- Expected state: `Running`
- Action: `cmd.exe /c "Y:\runtime\slurm_scheduler\start_web_y.cmd"`

Check that only one launcher exists:

```powershell
Get-CimInstance Win32_Process |
  Where-Object { $_.CommandLine -match 'start_web_y|slurm_scheduler' } |
  Select-Object ProcessId, ParentProcessId, CommandLine
Get-ScheduledTask -TaskName SlurmSchedulerWebNative
```

### SSH key path correction

Clean Git worktrees do not contain the untracked cluster keys. The runtime
`accounts.yaml` therefore uses absolute Y paths:

```text
Y:/git/slurm_scheduler/secrets/cluster/<account>.pem
```

Do not revert these to worktree-relative `secrets/cluster/...` paths; doing so makes
license snapshots and task/allocation refresh fail after a clean-worktree deployment.

### Live license/pool status at handoff

Immediately before writing this file:

- license snapshot valid: true
- Electronics Desktop: 344 / 550 used (value fluctuates)
- admission reserve: 32
- admission headroom: 173 at that snapshot
- central pool: `enabled=false`, `operational=false`
- canary UI limits: max AEDT 2, target projects 4, projects per AEDT 2
- central pool sessions/leases: 0/0

The current canary is deliberately node-local because compute nodes cannot reach the
Windows scheduler API. Probe task `30475` showed timeouts to all of:

- `100.112.168.31:8000`
- `192.168.0.3:8000`
- `192.168.56.1:8000`

Do not configure compute clients with any of these URLs. The node-local host publishes a
loopback URL on its compute node.

Health commands:

```powershell
Invoke-RestMethod http://127.0.0.1:8000/api/health
Invoke-RestMethod http://127.0.0.1:8000/api/licenses
Invoke-RestMethod http://127.0.0.1:8000/api/aedt-pool
Invoke-RestMethod http://127.0.0.1:8000/api/projects/MFT_1MW_2026v1
```

## 3. AEDT attach evidence and current canary

### Completed attach/isolation evidence

- Task `30089`: one real MFT project attached to one host AEDT; Matrix valid; project
  close ACK, Desktop exit and license return verified.
- Task `30445`: 1 AEDT / 2 real Matrix-only MFT projects; normal case and pre-solve
  client-abort case passed. Both valid results had `Llt=31.6777785986 µH`.
- Task `30474`: active-solve timeout fault passed. Client A was terminated, sibling B
  completed with `result_valid_em=1`, one leakage query and
  `Llt=31.6777785986 µH`; third client was rejected; faulted Desktop was recycled and
  Desktop/Maxwell features returned.
- MFT generic-N canary branch/SHA: `78d9511` (standalone remains the default; explicit
  canary acknowledgement required).

### Node-local host attempts

The host implementation is generic `N`; current validated bound is N=2. Persistent DB and
controller concepts are not named `1to2`.

Important task history:

- `30479`: failed before launch because its same-node parent `30473` was already failed.
- `30480`: cancelled before launch.
- `30481`: reached n046 but its raw task did not activate conda; exited before AEDT with
  `python: command not found`; no checkout.
- `30482`, `30483`: cancelled before launch during retry/race recovery.
- `30484`: successfully opened AEDT 2025.2 on n116 in 16.2 s and published:
  - Desktop PID 1052278
  - host PID 1051935
  - gRPC port 45193
  - loopback control plane `127.0.0.1:36359`
  - N=2
  - then its short-lived same-node anchor task `30371` completed, so scheduler marked
    host `30484` failed before clients were submitted.

Cleanup is proven complete. A direct `srun` cleanup on Slurm job 732393 verified the
`SLURM_SCHED_TASK_ID=30484` marker, terminated only Desktop PID 1052278, host PID 1051935
and persistence PID 1052342, and removed the bundle's `/tmp` files. Final n116 checks found
none of those PIDs, no task-30484 marker and no listener on port 45193. Clients `30485` and
`30486` failed before launch and had no simulation or license side effects. A fresh 01:58Z
license snapshot was valid and reported EDT 333; values naturally fluctuate.

The scheduler-specific handoff is also committed and pushed:

- `Y:\git\slurm_scheduler_node_canary_260713\docs\aedt_pool_handoff_20260713.md`
- docs-only branch tip `b5f8e725ec6810dad19f80bdedfd6089348eece2`
- live runtime remains the code SHA `bf273f4121356c0175de106375c14d1ee01619f6`

### Required next canary change

Do not tie the host lifecycle to a normal simulation task that may complete first. Use one
of these, preferably the first:

1. reserve the host directly on a live allocation without `same_node_as_task_id`, with the
   host task itself owning the node-placement lifetime; or
2. create a dedicated sentinel/anchor whose lifetime is the complete host plus N-client
   bundle and terminate it only after host close ACK.

Then submit two real MFT clients with:

- `scheduling_profile=fea_bursty`
- `aedt_backend=pooled`
- exact MFT SHA `78d9511`
- same node/allocation as the host
- explicit conda env setup (do not rely on the metadata name alone for a raw command)
- expected project count N=2 and the exact matching canary bundle identity

The clients must produce two terminal result rows, project-close ACKs, host/Desktop close
ACK, and before/during/after license evidence. If this fails, rollback only this canary and
return those cases to standalone; do not cancel existing production tasks.

## 4. Attach-aware refill controller (prepared, not live)

- Branch: `codex/pooled-refill-controller-260713`
- Final pushed SHA: `3106b0d16867394d9e193d38afd9653806dd8835`
- Worktree: `Y:\git\MFT_pooled_refill_controller_260713`
- New tests: 21 passed.
- Two old suite failures were reproduced unchanged at base `5f3987e` and are stale
  expectations, not introduced by this branch.

It implements:

- logical project target 300
- standalone/pooled both using `fea_bursty`
- generic `projects_per_aedt=N` and validated-N ceiling
- AEDT session ceiling and `ceil(projects/N)` capacity checks
- pooled bundle `expected_rows=N`
- missing result rows retried using new standalone identities (not reconciled to the same
  failed pooled task)
- no mass-cancel authority
- solver/library/scheduler/attach-validation/data-contract provenance in dedupe keys

Do not start it yet. It must be pinned to the final combined attach plus 1K101 solver SHA
and a new generation state; the mature controller currently authenticates old solver
`b171c7c`.

## 5. 1K101 / fill-factor status

### What is proven

- Native Maxwell Lamination material setup, segmented leg/yoke directions and Matrix solve
  reached solved state for kf 1.00, 0.85 and 0.70.
- Matrix times for the last run were approximately 9m30 / 5m21 / 2m35.
- The runs did **not** fail because of kf 0.70 material physics.

### Actual failure

- Branch: `experiment/1k101-native-lamination-ab-20260713`
- Current pushed SHA: `b04b470a3a92daa58cf4dbc17a333859198158dd`
  (`add tested Faraday core-loss gate pending physics approval`; supersedes
  `336ab7d` and the earlier `85e0963` attempt).
- Task `30473` (kf 0.70) failed during the independent `Bpow` postprocessor after Matrix.
- Tasks `30471` and `30472` were cancelled after a solved snapshot was preserved.
- Task `30478` was cancelled because a direct diagnostic was used instead.
- Direct diagnostic evidence (verify over direct SSH):
  `/home1/jji0930/slurm_scheduler/runs/mft_bpow_direct_s336ab7d_r3.log`
- `analyze_dispatched=false`; it reused the solved project.
- Exact result: `EnterQty(B)` passed in about 0.0005 s; the next
  `CalcOp(ComplxPeak)` immediately caused abnormal AEDT macro/gRPC termination. Division,
  fractional power and integration were never reached.
- The previous long delay was three blind retries waiting on AEDT macro timeout, not FEA
  solve time.
- Saved kf 0.85 project snapshot (verify over direct SSH):
  `/home1/jji0930/slurm_scheduler/runs/mft_postprocess_snapshot_30472_s85e0963`
- Direct diagnostic closed; last reported Desktop usage after it was 371/550.

### Correct validation direction from the user

Do **not** use AEDT Field Calculator as the truth source for validating the loss equation.
The calculator may be used only for ordinary result extraction.

Implement an independent Python reference:

```text
B_pack     = Faraday(Vrms, f, N, gross core area)
B_material = B_pack / kf
specific loss = 6.5 * (f_kHz ** 1.51) * (B_material ** 1.74)  [W/kg]
raw core loss = specific loss * effective alloy mass
margin core loss = raw core loss * configured margin
```

Compare this independent result against Maxwell native Lamination B/CoreLoss. Optional
raw B-field export may be added later for a distribution check; do not recreate the
`B^1.74` truth gate inside AEDT calculator.

The independent reference implementation is committed in `b04b470` across six files:
`module/core_material_contract.py`, `run_simulation_260706.py`,
`regression_260707/quality_contract.py`, `regression_260707/verify/scheduler_client.py`,
`regression_260707/verify/1k101_native_ab_gate.json`, and
`regression_260707/test_core_material_contract.py`. It removes active Bpow/ComplxPeak
creation, adds the Python Faraday/POWERLITE reference, and updates gates to B relative
error <= 0.15 and raw-loss relative error <= 0.30. Verification completed: py_compile,
`git diff --check`, 32+22 core-material tests and 202+58 stability/integrity tests passed.
The branch is clean and matches origin. This proves implementation consistency only; the
saved-solution numerical comparison and physics approval are still pending.

Try to reuse the saved kf 0.85 solution and the failed task 30473's solved project for kf
0.70 native B/CoreLoss extraction before rerunning Matrix. Only rerun if the saved solution
is unavailable.

Production application rule:

- If the independent gate passes and kf=0.70 is selected, create a new material/data
  revision (suggested `mft1mw-1k101-native-lamination-kf0p70-v3`).
- Put kf explicitly in every params payload and dedupe key even if 0.70 is the default.
- B, core-loss, total-loss and dependent thermal targets must use the new revision.
- Llt and winding-loss legacy reuse is allowed only through explicit target-aware cohort
  metadata.
- Do not mix old and new B/core-loss rows in one model.

## 6. Monitoring/data status

- Live monitoring source: `Y:\git\MFT_monitoring_throughput_12bc`
- Live monitoring SHA: `12bcf82be2649488b96f85e5926778ec914c94f1`
- Port: 8010
- Timing/parity/model UI, zero-aware MAPE and solver-inflow-vs-strict-growth split were
  already deployed.
- Current strict status file recorded:
  - raw rows 1543
  - strict EM 1087
  - strict full 1014
  - quarantined 529
  - activation minimum 3000
  - solver/library identity `b171c7c` / `e6b9b9d`
- The monitoring status may temporarily show scheduler timeout/zero counts while scheduler
  performs a long refresh tick. Cross-check the scheduler project API before believing 0.

The existing WEB project deployment record is stale and `auto_pull=false`; current tasks
do not use it. Production/canary task commands fetch exact GitHub SHAs and detach them.

## 7. Controller/campaign state

- MFT project max active: 300.
- Old controller is stopped intentionally.
- Last reconciled interrupted cycle: cycle 467, reconciled committed with accepted task
  IDs 30469/30470 and evidence SHA
  `f32bb28fbec01506663a1a2b0000c8c578806963d811c23b90bc152f2b6cc852`.
- Do not restart
  `regression_260707/logs/controller_release_6a870_runtime/run_controller_6a870.py`
  until the final solver/material revision and attach policy are selected.

When restarting with the new controller, preserve these invariants:

- logical project active target exactly 300
- no mass cancel and no all-at-once node return
- rolling replacement only
- pooled failure retries only missing cases as new standalone identities
- strict dataset accepts only authenticated exact revisions

## 8. Git branches to preserve, then consolidate later

Do not delete branches while active/finished tasks still reference their SHAs. The user
asked to clean branch sprawl only after stabilization.

Preserve at least:

- scheduler `experiment/aedt-node-canary-260713` @ `bf273f4`
- scheduler `feature/aedt-backend-selector-20260713` @ `9f7b005`
- MFT attach canary @ `78d9511`
- MFT attach-aware controller `codex/pooled-refill-controller-260713` @ `3106b0d`
- 1K101 `experiment/1k101-native-lamination-ab-20260713` @ `b04b470`
- monitoring throughput @ `12bcf82`
- production standalone solver/library `b171c7c` / `e6b9b9d`

After attach plus 1K101 stabilize, merge deployable changes into one scheduler release
branch and one MFT solver release branch, run full tests, switch pinned campaign SHA, then
archive/delete superseded experiment branches only after their task/evidence references
are recorded.

## 9. Immediate next-action checklist

1. Fix canary host lifetime so it does not depend on a short simulation anchor. The
   smallest next attempt is a high-priority host with `same_node_as=0` placed on a normal
   warm allocation; clients then use `same_node_as=<host task>`.
2. Run one N=2 node-local canary with two real pooled MFT clients to terminal rows and
   close/license evidence.
3. Finish the pure-Python Faraday/POWERLITE reference and compare saved kf 0.85/0.70
   native outputs.
4. Create one combined MFT attach plus approved-1K101 revision and its target-aware data
   contract.
5. Run the prepared attach-aware controller in read-only plan mode; review exact
   project/AEDT counts and generation identity.
6. Start rolling refill toward logical active 300; do not mass cancel.
7. Confirm strict-full growth resumes and continue until 3000 authenticated rows.
8. Only after runtime stability, consolidate Git branches.
