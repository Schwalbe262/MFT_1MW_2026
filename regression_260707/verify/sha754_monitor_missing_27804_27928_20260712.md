# SHA754 thermal `monitor_missing` forensic summary

Recorded 2026-07-12 KST from one authorized, bounded terminal-stderr request per
failed task (`tail_lines=200`, `max_bytes=65536`). No stdout was requested and
these stderr endpoints must not be queried again for this audit.

## Observed terminal tails

| task | node / allocation / account | bounded response | Icepak sequence |
|---|---|---:|---|
| 27804 | n116 / 7936 / jji0930 | 131 lines, 9,159 bytes | `No mesh operation found`; `No mesh region found`; `Property Command is read-only`; `AnalyzeAll` error; no fresh monitor; one retry; second `AnalyzeAll` error; iteration 0 / `monitor_missing` |
| 27928 | n107 / 8100 / harry261 | 130 lines, 9,201 bytes | Same sequence as 27804: no mesh operation/region, read-only Command property, two failed `AnalyzeAll` calls around the single retry, then iteration 0 / `monitor_missing` |

Both tails show Maxwell completing beforehand and Icepak model/material,
thermal-source, symmetry-wall, inlet, and outlet creation completing. Each
Icepak `AnalyzeAll` attempt returned an error after about one minute. Neither
tail contains a license-denial, out-of-memory, killed process, segmentation
fault, explicit mesh-generation exception, or thermal cell-count line. The
parquet-engine warning occurs after thermal rejection and is not its cause.

## Correlation assessment

- Cached fingerprint for both tasks: `d44e42386932a56a` (count 2; fail-closed
  pause threshold 3).
- The failures occurred on different nodes, allocations, Slurm jobs, and user
  accounts, so a single-node or single-allocation fault is not supported.
- A broad license outage is not evidenced by either tail. It cannot be ruled
  out from these bounded logs alone, but task 27758 completed while its runtime
  overlapped both failures.
- The repeated pre-solve warnings and iteration-0 result point more strongly to
  Icepak setup/mesh initialization or AEDT `AnalyzeAll` startup than to ordinary
  thermal non-convergence after mesh/cell iteration.
- Geometry correlation is provisional: both failures use `N1=8`, total `N2=80`,
  `h1=689/638`, `n_core_group=3/2`, and `core_plate_t=29.6/24.4`; the sole valid
  terminal comparator 27758 uses `N1=5`, total `N2=50`, `h1=392`,
  `n_core_group=5`, and `core_plate_t=13.6`. Two failures are insufficient to
  identify a causal geometry field.

Current guard state at capture: `paused=false`, no pause reasons, action
`wait_local3`. A third `d44e42386932a56a` terminal outcome must latch the
repeated-runtime-error pause before any refill.

## Guard activation addendum

At 2026-07-12 09:27 KST, cached controller classification added task 27794 as
the third `d44e42386932a56a` outcome. The controller advanced to revision 3,
latched `paused=true` with
`repeated_runtime_error:d44e42386932a56a:3`, selected
`manual_intervention`, and performed no mutation.

No additional stderr was requested for this addendum. Scheduler metadata shows
that 27794 and the separate AEDT-session `TypeError` failure 27799 shared node
n116, allocation 7936, Slurm job 731045, and nearly identical start/finish
times. Task 27804 also used that allocation/job. This makes burst/session
instability a plausible contributor for those three tasks, but it cannot be the
complete `d44e` explanation because task 27928 failed on n107/allocation 8100,
while valid task 27763 also completed on n107.

Task 27794 uses `N1=7`, total `N2=70`, `h1=422`, `n_core_group=4`, and
`core_plate_t=23.1`; therefore the earlier exact `N1=8` / total `N2=80`
geometry association is disproved. All three `d44e` cases do have relatively
thick core plates (23.1, 24.4, and 29.6 mm), but three cases remain insufficient
to infer causality.

One subsequently authorized bounded stderr request was made for each new
failure (27794: 130 lines / 9,203 bytes; 27799: 131 lines / 9,357 bytes). Task
27794 exactly repeats the prior `d44e` sequence, including the mesh-operation
and mesh-region warnings, two failed `AnalyzeAll` calls, and iteration-0
`monitor_missing`. No thermal cell count is logged.

Task 27799 also contains that complete sequence before its cached classifier
message: lines 99--119 show the same mesh warnings, two failed `AnalyzeAll`
calls, and iteration-0 `monitor_missing`. After the project closes, line 128
adds a one-line PyAEDT `TypeError` while retrieving active-session information;
there is no traceback in the bounded tail. This occurs during Desktop cleanup,
not before the thermal failure. The reverse stderr classifier selected that
later cleanup line and assigned fingerprint `1fb366cff3339b71`, masking the
underlying fourth `d44e` thermal failure. The controller was already safely
paused by the third classified `d44e`, but future classification should
prioritize the explicit thermal rejection over post-close cleanup errors.
